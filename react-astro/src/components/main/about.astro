---
import ReactSkill from "@/components/skills/react";
import TypescriptSkill from "@/components/skills/typescript";

import FirebaseSkill from "../skills/firebase";
import styles from "./Main.module.css";
---

<section class={styles.section}>
  <h2 class={styles.sectionHeading}>About me</h2>
  <div class="flex flex-wrap items-center gap-x-1 leading-8">
    Fullstack developer, specializing in interactive web apps with
    <ReactSkill client:load size="sm" />,
    <TypescriptSkill client:load size="sm" /> and
    <FirebaseSkill client:load size="sm" />.
  </div>
</section>
