---
import SVGTooltip from "@/components/svg-tooltip";
import {
  Card,
  CardBadges,
  CardDescription,
  CardFooter,
  CardRepoLink,
  CardHeader,
  CardImage,
  CardDemoLink,
  CardSkills,
  CardTitle,
} from "@/components/ui/card";
import type { Project } from "@/lib/projects";

type Props = {
  project: Project;
  showBadges?: boolean;
};

const { project, showBadges = false } = Astro.props;
---

<Card>
  <CardHeader>
    {
      showBadges && (
        <CardBadges>
          {project.badges.map((badge) => {
            const Badge = badge;
            return <Badge />;
          })}
        </CardBadges>
      )
    }
    <CardImage src={project.image} alt={project.name} />
    <CardTitle>{project.name}</CardTitle>
    <CardDescription>{project.description}</CardDescription>
  </CardHeader>
  <CardSkills>
    {
      project.techStack.map((tech) => (
        <SVGTooltip client:visible content={tech.title}>
          <tech.icon />
        </SVGTooltip>
      ))
    }
  </CardSkills>
  <CardFooter>
    <CardRepoLink githubUrl={project.repoUrl} />
    <CardDemoLink demoUrl={project.demoUrl} />
  </CardFooter>
</Card>
